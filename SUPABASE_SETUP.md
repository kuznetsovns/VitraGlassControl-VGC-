# Настройка Supabase для хранения ID витражей

## Что было сделано

Создана инфраструктура для хранения размещенных витражей и их ID в базе данных Supabase вместо локального хранилища браузера.

### Новые таблицы

1. **placed_vitrages** - хранит размещенные витражи на планах этажей с полным ID
2. **vitrage_segment_ids** - хранит индивидуальные ID для каждого сегмента витража

### Преимущества

✅ Данные доступны с любого компьютера
✅ Данные сохраняются между сессиями
✅ Витражи с назначенными ID автоматически попадают во вкладку "Дефектовка"
✅ Централизованное хранение всех данных по проекту

## Инструкция по применению миграций

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на [app.supabase.com](https://app.supabase.com/)
2. Войдите в свой аккаунт
3. Выберите ваш проект

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите **SQL Editor**
2. Нажмите **New Query** для создания нового запроса

### Шаг 3: Примените миграции

Необходимо выполнить 2 миграции в следующем порядке:

#### Миграция 1: placed_vitrages (007)

1. Откройте файл `supabase/migrations/007_create_placed_vitrages_table.sql`
2. Скопируйте **весь** содержимое файла
3. Вставьте в SQL Editor
4. Нажмите **RUN** или `Ctrl+Enter`
5. Дождитесь сообщения об успешном выполнении

#### Миграция 2: vitrage_segment_ids (008)

1. Откройте файл `supabase/migrations/008_create_vitrage_segment_ids_table.sql`
2. Скопируйте **весь** содержимое файла
3. Вставьте в SQL Editor (в новой вкладке или очистите предыдущую)
4. Нажмите **RUN** или `Ctrl+Enter`
5. Дождитесь сообщения об успешном выполнении

### Шаг 4: Проверка

Выполните следующий SQL запрос для проверки:

```sql
-- Проверка создания таблиц
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name IN ('placed_vitrages', 'vitrage_segment_ids');
```

Вы должны увидеть 2 строки:
- `placed_vitrages`
- `vitrage_segment_ids`

## Как это работает

### При размещении витража на плане этажа:

1. Вы размещаете витраж на плане этажа
2. Нажимаете кнопку "🆔 Задать ID секций"
3. Заполняете 8-компонентный ID для каждого сегмента
4. **ID автоматически сохраняются в Supabase**

### При создании дефектовки:

1. Кнопка "📋 Создать дефектовку" становится доступной после назначения всех ID
2. При открытии дефектовки витраж отображается с реальными ID сегментов
3. Все дефекты сохраняются в Supabase
4. **Витражи с дефектами автоматически появляются во вкладке "Дефектовка"**

### Во вкладке "Дефектовка":

1. Отображаются только витражи с назначенными ID
2. Можно фильтровать по объекту, корпусу, этажу и т.д.
3. Отображается статус проверки и количество дефектов
4. Все изменения синхронизируются между пользователями

## Формат ID витража

ID состоит из 8 компонентов, разделенных дефисом:

```
Объект-Корпус-Секция-Этаж-Квартира-НомерВитража-НазваниеВитража-СекцияВитража
```

Пример:
```
Зил18-КорпусА-Секция1-2-101-1-В-01
```

## Миграция данных из localStorage

Если у вас уже есть витражи в localStorage, они продолжат работать, но не будут синхронизироваться между устройствами. Для миграции:

1. Откройте план этажа с витражами
2. Для каждого витража назначьте ID заново
3. ID автоматически сохранятся в Supabase
4. Старые данные из localStorage можно очистить

## Резервное копирование

Supabase автоматически создает резервные копии вашей базы данных. Дополнительно вы можете:

1. Экспортировать данные через Dashboard → Database → Tables
2. Создать дамп базы через SQL:

```sql
-- Экспорт витражей
SELECT * FROM placed_vitrages;

-- Экспорт ID сегментов
SELECT * FROM vitrage_segment_ids;
```

## Поддержка

Если возникли вопросы или ошибки при применении миграций:

1. Проверьте логи в Supabase Dashboard → Database → Logs
2. Убедитесь, что миграции 001-006 уже применены
3. Проверьте, что у вас есть права на создание таблиц в проекте

## Откат изменений

Если нужно откатить миграции:

```sql
-- Удалить таблицу vitrage_segment_ids
DROP TABLE IF EXISTS vitrage_segment_ids CASCADE;

-- Удалить таблицу placed_vitrages
DROP TABLE IF EXISTS placed_vitrages CASCADE;
```

⚠️ **ВНИМАНИЕ:** Это удалит все данные в этих таблицах!
